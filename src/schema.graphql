  type Subscription {
    eventAdded: Event!
  }
  type Query {
    events: [Event!]!
    feed: String!
  }
  type Mutation {
    createEvent(topic: String!, message: String!): Event!
  }
  type Event {
    _id: ID!
    topic: String!
    message: String!
  }
